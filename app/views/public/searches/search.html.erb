<div class='container'>
  <div class='row'>
      <% if @range == "Customer" %>
        <%= render 'searches/searchCustomers', word: @word, customers: @customers %>
      <% else %>
        <%= render 'searches/searchPosts', word: @word, posts: @posts %>
      <% end %>
  </div>
</div>